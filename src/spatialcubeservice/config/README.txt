This directory contains configuration files specific to the web services. These
may override the config files from the StorageManager if desired; to do so:
 1. Create a file of the same name here.
 2. Exclude that file from the linked StorageManagerConfig directory.
