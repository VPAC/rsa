The files in this directory override those in ../../StorageManager/config. Note
that it's not marked as a source directory in Eclipse, so it's only used in
release mode (i.e. after building with ant).
